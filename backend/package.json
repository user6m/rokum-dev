{
  "name": "@rokum-dev/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest || exit 0",
    "test:ci": "vitest run --reporter=dot || exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "20.4.2",
    "tsx": "^4.7.0",
    "typescript": "5.3.3",
    "vitest": "^1.3.8"
  }
}

